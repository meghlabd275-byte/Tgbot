# Requires Python 3.13-compatible release: PTB 20.7/20.8 and 21.2/21.3
# crash at Application.Builder().build() on Python 3.13 due to a __slots__
# mismatch (AttributeError: 'Updater' object has no attribute
# '_Updater__polling_cleanup_cb'). The regression was fixed in 21.4.
python-telegram-bot>=21.4,<22
sqlalchemy>=2.0.23
aiosqlite==0.19.0
python-dotenv==1.0.0
pytz==2023.3.post1
flask==3.0.0
requests==2.31.0
urllib3>=2.1.0
# psycopg3 is the modern Postgres driver (with prebuilt wheels) used by
# SQLAlchemy for PostgreSQL database connections.
psycopg[binary]>=3.1,<4
# PyMySQL is the pure-Python MySQL/MariaDB driver used by SQLAlchemy.
PyMySQL>=1.1,<2
# cryptography enables SHA-256 password auth for MySQL 8+.
cryptography>=41
