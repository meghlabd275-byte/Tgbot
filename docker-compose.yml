version: '3.8'

services:
  telegram-bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DATABASE_URL=${DATABASE_URL:-sqlite:///data/bot.db}
    volumes:
      - ./data:/app/data